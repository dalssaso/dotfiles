;;; ayu-palette.el --- Generated from .chezmoidata/palette.toml -*- lexical-binding: t; -*-
;; Do not edit. Regenerated by chezmoi apply.

(defconst hd/ayu-palette
  '((dark . ((bg0 . "{{ .palette.dark.bg0 }}")
             (fg . "{{ .palette.dark.fg }}")
             (red . "{{ .palette.dark.red }}")
             (orange . "{{ .palette.dark.orange }}")
             (yellow . "{{ .palette.dark.yellow }}")
             (green . "{{ .palette.dark.green }}")
             (aqua . "{{ .palette.dark.aqua }}")
             (blue . "{{ .palette.dark.blue }}")
             (purple . "{{ .palette.dark.purple }}")
             (grey0 . "{{ .palette.dark.grey0 }}")
             (grey1 . "{{ .palette.dark.grey1 }}")))
    (light . ((bg0 . "{{ .palette.light.bg0 }}")
              (fg . "{{ .palette.light.fg }}")
              (red . "{{ .palette.light.red }}")
              (orange . "{{ .palette.light.orange }}")
              (yellow . "{{ .palette.light.yellow }}")
              (green . "{{ .palette.light.green }}")
              (aqua . "{{ .palette.light.aqua }}")
              (blue . "{{ .palette.light.blue }}")
              (purple . "{{ .palette.light.purple }}")
              (grey0 . "{{ .palette.light.grey0 }}")
              (grey1 . "{{ .palette.light.grey1 }}"))))
  "Ayu palette roles per variant.")

(defun hd/ayu-color (variant role)
  "Return the hex for ROLE in VARIANT."
  (alist-get role (alist-get variant hd/ayu-palette)))

(provide 'ayu-palette)
