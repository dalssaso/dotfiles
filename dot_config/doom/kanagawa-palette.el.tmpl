;;; kanagawa-palette.el --- Generated from .chezmoidata/palette.toml -*- lexical-binding: t; -*-
;; Do not edit. Regenerated by chezmoi apply.

(defconst hd/kanagawa-palette
  '((dragon . ((bg0 . "{{ .palette.dragon.bg0 }}")
               (fg . "{{ .palette.dragon.fg }}")
               (red . "{{ .palette.dragon.red }}")
               (orange . "{{ .palette.dragon.orange }}")
               (yellow . "{{ .palette.dragon.yellow }}")
               (green . "{{ .palette.dragon.green }}")
               (aqua . "{{ .palette.dragon.aqua }}")
               (blue . "{{ .palette.dragon.blue }}")
               (purple . "{{ .palette.dragon.purple }}")
               (grey0 . "{{ .palette.dragon.grey0 }}")
               (grey1 . "{{ .palette.dragon.grey1 }}")))
    (lotus . ((bg0 . "{{ .palette.lotus.bg0 }}")
              (fg . "{{ .palette.lotus.fg }}")
              (red . "{{ .palette.lotus.red }}")
              (orange . "{{ .palette.lotus.orange }}")
              (yellow . "{{ .palette.lotus.yellow }}")
              (green . "{{ .palette.lotus.green }}")
              (aqua . "{{ .palette.lotus.aqua }}")
              (blue . "{{ .palette.lotus.blue }}")
              (purple . "{{ .palette.lotus.purple }}")
              (grey0 . "{{ .palette.lotus.grey0 }}")
              (grey1 . "{{ .palette.lotus.grey1 }}"))))
  "Kanagawa palette roles per variant.")

(defun hd/kanagawa-color (variant role)
  "Return the hex for ROLE in VARIANT."
  (alist-get role (alist-get variant hd/kanagawa-palette)))

(provide 'kanagawa-palette)
